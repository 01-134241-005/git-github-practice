# Git Notes

## Merge vs Rebase

Merge combines changes from one branch into another branch and keeps the branch history. It may create a merge commit.

Rebase moves commits from one branch and places them on top of another branch. It creates a cleaner and more linear history.

## Merge Conflict

A merge conflict happens when two branches change the same part of the same file and Git cannot decide which version to keep.
